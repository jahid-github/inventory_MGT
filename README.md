# inventory_MGT

## Run With Docker

Install Docker Desktop, then run this from the project folder:

```powershell
docker compose up --build
```

Open:

```text
http://localhost:8000
```

Full setup and troubleshooting steps are in [DOCKER_USER_GUIDE.md](DOCKER_USER_GUIDE.md).
